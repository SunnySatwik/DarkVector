# DV Sentinel — Python dependencies
# Install with: pip install -r requirements.txt

# HTTP client — async-capable, modern replacement for requests
httpx>=0.27.0

# System metrics — CPU, memory, uptime, network interfaces
psutil>=5.9.0
